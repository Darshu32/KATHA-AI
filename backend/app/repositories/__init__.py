"""Concrete repository implementations.

Stage 1 ships pricing repositories under ``app.repositories.pricing``.
Later stages add more (themes, codes, materials, …).

Usage::

    from app.repositories.pricing import (
        MaterialPriceRepository,
        LaborRateRepository,
        TradeHourRepository,
        CityPriceIndexRepository,
        CostFactorRepository,
        PricingSnapshotRepository,
    )
"""
