# ── Core ─────────────────────────────────────────────
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.11.2
pydantic-settings==2.8.1

# ── Database ─────────────────────────────────────────
sqlalchemy[asyncio]==2.0.38
asyncpg==0.30.0
alembic==1.14.1

# ── Auth ─────────────────────────────────────────────
passlib[bcrypt]==1.7.4
PyJWT==2.10.1

# ── AI ───────────────────────────────────────────────
# Anthropic Claude is the PRIMARY agent runtime (Stage 2+).
# OpenAI is fallback + embeddings + legacy structured-output paths.
anthropic==0.42.0
openai==1.68.0

# ── Background Jobs ─────────────────────────────────
celery[redis]==5.4.0
redis==5.2.1

# ── Storage (S3/R2 compatible) ───────────────────────
boto3==1.36.14

# ── Vector search ───────────────────────────────────
pgvector==0.3.6

# ── Document extraction (Stage 6 RAG corpus) ────────
# PyMuPDF for PDF text + page-level metadata extraction.
# Imported lazily inside app/corpus/extractors/pdf.py so the rest
# of the codebase (and tests using synthetic plain-text fixtures)
# can run without it installed.
PyMuPDF==1.24.13

# ── Document generation (Phase 1 Pass A exporters) ──
reportlab==4.4.10
python-docx==1.2.0
openpyxl==3.1.5

# ── CAD / 3D exporters (Phase 1 Pass B) ─────────────
ezdxf==1.4.3
# OBJ + GLTF written without extra deps (pure stdlib)

# ── BIM / specialist exporters (Phase 1 Pass C) ─────
ifcopenshell==0.8.4.post1
# STEP + G-code written without extra deps (pure stdlib)

# ── Dev / Testing ───────────────────────────────────
httpx==0.28.1
pytest==8.3.4
pytest-asyncio==0.25.3
