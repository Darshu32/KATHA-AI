{
  "name": "katha-ai",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev:web": "npm --workspace @katha/web run dev",
    "build:web": "npm --workspace @katha/web run build"
  }
}

